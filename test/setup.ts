// Global Vitest setup. `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` are stubbed
// via vitest.config.ts's `test.env` so lib/supabase.ts's eager createClient()
// call never throws during test collection.
