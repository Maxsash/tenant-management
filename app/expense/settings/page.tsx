import ExpenseSettings from "@/components/expenses/ExpenseSettings";

export const metadata = {
  title: "Expense Settings - Shrivastava Hub",
};

export default function Page() {
  return <ExpenseSettings />;
}
